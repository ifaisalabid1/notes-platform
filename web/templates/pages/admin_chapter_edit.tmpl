{{ define "content" }}
  <section class="mx-auto max-w-3xl space-y-8">
    <div>
      <span class="badge">Edit Content</span>
      <h1 class="page-title mt-4">Edit Chapter</h1>
      <p class="page-subtitle">
        Update chapter details, parent unit, and publishing status.
      </p>
    </div>

    {{ if .Error }}
      <div class="alert-error" role="alert">{{ .Error }}</div>
    {{ end }}


    <section class="card">
      <form
        method="post"
        action="/admin/chapters/{{ .Data.Chapter.ID }}/edit"
        class="space-y-5"
      >
        <input type="hidden" name="csrf_token" value="{{ .CSRFToken }}" />

        <div>
          <label for="unit_id">Unit</label>
          <select id="unit_id" name="unit_id" required>
            {{ range .Data.Units }}
              <option
                value="{{ .ID }}"
                {{ if eq .ID $.Data.Chapter.UnitID }}selected{{ end }}
              >
                {{ .ClassName }} - {{ .SemesterName }} - {{ .SubjectName }} -
                {{ .Name }}
              </option>
            {{ end }}
          </select>
        </div>

        <div class="form-grid">
          <div>
            <label for="name">Chapter Name</label>
            <input
              id="name"
              name="name"
              type="text"
              value="{{ .Data.Chapter.Name }}"
              required
            />
          </div>

          <div>
            <label for="sort_order">Sort Order</label>
            <input
              id="sort_order"
              name="sort_order"
              type="number"
              value="{{ .Data.Chapter.SortOrder }}"
            />
          </div>
        </div>

        <div>
          <label for="description">Description</label>
          <textarea id="description" name="description" rows="4">
{{ if .Data.Chapter.Description }}
              {{ .Data.Chapter.Description }}
            {{ end }}</textarea
          >
        </div>

        <label class="inline-flex items-center gap-2">
          <input
            name="is_published"
            type="checkbox"
            class="h-4 w-4 rounded border-academic-300"
            {{ if .Data.Chapter.IsPublished }}checked{{ end }}
          />
          <span>Published</span>
        </label>

        <div class="flex flex-wrap gap-3">
          <button type="submit" class="btn-primary">Save Changes</button>
          <a href="/admin/chapters" class="btn-secondary">Cancel</a>
        </div>
      </form>
    </section>
  </section>
{{ end }}
