{{ define "content" }}
  <section class="space-y-8">
    <div>
      <span class="badge">Content Management</span>
      <h1 class="page-title mt-4">Classes</h1>
      <p class="page-subtitle">
        Create the top-level classes for your notes platform.
      </p>
    </div>

    {{ if .Error }}
      <div class="alert-error" role="alert">{{ .Error }}</div>
    {{ end }}


    <section class="card">
      <h2 class="section-title">Create Class</h2>

      <form method="post" action="/admin/classes" class="mt-6 space-y-5">
        <input type="hidden" name="csrf_token" value="{{ .CSRFToken }}" />

        <div class="form-grid">
          <div>
            <label for="name">Class Name</label>
            <input
              id="name"
              name="name"
              type="text"
              placeholder="Example: BCA, MCA, Class 10"
              required
            />
          </div>

          <div>
            <label for="sort_order">Sort Order</label>
            <input id="sort_order" name="sort_order" type="number" value="0" />
          </div>
        </div>

        <div>
          <label for="description">Description</label>
          <textarea
            id="description"
            name="description"
            rows="3"
            placeholder="Optional short description"
          ></textarea>
        </div>

        <label class="inline-flex items-center gap-2">
          <input
            name="is_published"
            type="checkbox"
            class="h-4 w-4 rounded border-academic-300"
          />
          <span>Published</span>
        </label>

        <div>
          <button type="submit" class="btn-primary">Create Class</button>
        </div>
      </form>
    </section>

    <section class="space-y-4">
      <h2 class="section-title">Existing Classes</h2>

      {{ with .Data.Classes }}
        <div class="table-wrap overflow-x-auto">
          <table>
            <thead>
              <tr>
                <th>Name</th>
                <th>Slug</th>
                <th>Published</th>
                <th>Sort Order</th>
                <th>Created At</th>
                <th>Actions</th>
              </tr>
            </thead>

            <tbody>
              {{ range . }}
                <tr>
                  <td class="font-medium text-academic-900">{{ .Name }}</td>
                  <td>{{ .Slug }}</td>
                  <td>{{ if .IsPublished }}Yes{{ else }}No{{ end }}</td>
                  <td>{{ .SortOrder }}</td>
                  <td>{{ .CreatedAt.Format "2006-01-02 15:04" }}</td>
                  <td>
                    <a href="/admin/classes/{{ .ID }}/edit">Edit</a>
                  </td>
                </tr>
              {{ end }}
            </tbody>
          </table>
        </div>
      {{ else }}
        <div class="card-muted">
          <p class="text-sm text-academic-600">No classes found.</p>
        </div>
      {{ end }}
    </section>

    <a href="/admin/dashboard" class="btn-secondary">Back to dashboard</a>
  </section>
{{ end }}
