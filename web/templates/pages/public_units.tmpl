{{ define "content" }}
  <section class="space-y-8">
    <div>
      <nav class="text-sm text-academic-500">
        <a href="/" class="nav-link">Classes</a>
        <span class="mx-2">/</span>
        <a href="/classes/{{ .Data.Class.Slug }}" class="nav-link"
          >{{ .Data.Class.Name }}</a
        >
        <span class="mx-2">/</span>
        <a
          href="/classes/{{ .Data.Class.Slug }}/semesters/{{ .Data.Semester.Slug }}"
          class="nav-link"
          >{{ .Data.Semester.Name }}</a
        >
        <span class="mx-2">/</span>
        <span>{{ .Data.Subject.Name }}</span>
      </nav>

      <h1 class="page-title mt-4">{{ .Data.Subject.Name }}</h1>

      {{ if .Data.Subject.Description }}
        <p class="page-subtitle">{{ .Data.Subject.Description }}</p>
      {{ else }}
        <p class="page-subtitle">Choose a unit to continue.</p>
      {{ end }}
    </div>

    {{ with .Data.Units }}
      <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {{ range . }}
          <a
            href="/classes/{{ $.Data.Class.Slug }}/semesters/{{ $.Data.Semester.Slug }}/subjects/{{ $.Data.Subject.Slug }}/units/{{ .Slug }}"
            class="card group no-underline hover:border-brand-300 hover:shadow-md"
          >
            <div class="flex items-start justify-between gap-4">
              <div>
                <h2
                  class="text-lg font-semibold text-academic-900 group-hover:text-brand-700"
                >
                  {{ .Name }}
                </h2>

                {{ if .Description }}
                  <p class="mt-2 text-sm leading-6 text-academic-600">
                    {{ .Description }}
                  </p>
                {{ else }}
                  <p class="mt-2 text-sm leading-6 text-academic-600">
                    View chapters for this unit.
                  </p>
                {{ end }}
              </div>

              <span class="badge">Unit</span>
            </div>
          </a>
        {{ end }}
      </div>
    {{ else }}
      <div class="empty-state">
        <h3 class="empty-state-title">Nothing available yet</h3>
        <p class="empty-state-text">
          Content will appear here once it is published by the admin.
        </p>
      </div>
    {{ end }}
  </section>
{{ end }}
